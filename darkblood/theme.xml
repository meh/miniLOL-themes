<?xml version="1.0" encoding="utf-8"?>

<!DOCTYPE theme [
    <!ELEMENT theme (file*)>
    <!ATTLIST theme
        name   CDATA #REQUIRED
        author CDATA #REQUIRED
    >
]>

<theme name="Dark Blood" author="meh." description="One of the meh's homepage themes." menu="head" content="body">
    <initialize><![CDATA[
        miniLOL.theme.resize = function () {
            $('body').setStyle({
                height: (document.viewport.getHeight()-(50+$('head').getHeight()+20)-(50+$('credits').getHeight()+20))+'px'
            });
        };

        miniLOL.theme.resize();
        Event.observe(window, 'resize', miniLOL.theme.resize);
        Event.observe(document, ':initialized', miniLOL.theme.resize);
    ]]></initialize>

    <finalize><![CDATA[
        Event.stopObserving(window, 'resize', miniLOL.theme.resize);
        delete miniLOL.theme.resize;
    ]]></finalize>

    <styles>
        <style name="core"/>
        <style name="menu"/>

        <style name="blog/style"/>

        <style name="SyntaxHighlighter/style"/>
    </styles>
</theme>
